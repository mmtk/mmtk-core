window.SIDEBAR_ITEMS = {"trait":[["CoordinatorWork","A special kind of work that will execute on the coordinator (i.e. controller) thread"],["GCWork",""],["GCWorkContext","This trait provides a group of associated types that are needed to create GC work packets for a certain plan. For example, `GCWorkScheduler.schedule_common_work()` needs this trait to schedule different work packets. For certain plans, they may need to provide several types that implement this trait, e.g. one for nursery GC, one for mature GC."]]};