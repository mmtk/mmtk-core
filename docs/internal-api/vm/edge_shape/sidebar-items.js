initSidebarItems({"struct":[["SimpleEdge","A simple edge implementation that represents a word-sized slot where an ObjectReference value is stored as is.  It is the default edge type, and should be suitable for most VMs."]],"trait":[["Edge","An abstract edge.  An edge holds an object reference.  When we load from it, we get an ObjectReference; we can also store an ObjectReference into it."]]});