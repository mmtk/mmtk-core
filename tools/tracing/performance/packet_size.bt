usdt:$MMTK:mmtk:process_slots {
    if (@stats_enabled) {
        @process_slots_packet_size = hist(arg0);
        if (arg1) {
            @process_slots_root_packet_size = hist(arg0);
        }
    }
}
